server.port=8084
spring.application.name=match-service

# Habilitar hilos virtuales de Java 21
spring.threads.virtual.enabled=true

# Conexión a Base de Datos MySQL 8
spring.datasource.url=jdbc:mysql://localhost:3306/db_sanosysalvos_match?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=patsYned24
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Clave de API de OpenAI (clave ofuscada para seguridad, el ms funcionará correctamente si se proporciona la clave real)
openai.api.key=${OPENAI_API_KEY}